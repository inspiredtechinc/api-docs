openapi: 3.0.0
info:
  title: Mobile Service API
  description: API documentation for the Mobile service in staging environment.
  version: 1.0.0
paths:
  /example:
    get:
      summary: Example endpoint
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "This is an example response."